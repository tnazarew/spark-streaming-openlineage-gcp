# OpenLineage GCP transport configuration (required)
# This file MUST exist on the test classpath and contain non-empty values.
# Fill all keys below before running tests.
#
# Required keys (must be non-empty):
#   ol.gcp.projectId
#   ol.gcp.location
#   ol.gcp.credentialsFile
#
# Example:
# ol.gcp.projectId=my-gcp-project
# ol.gcp.location=us
# ol.gcp.credentialsFile=/absolute/path/to/service-account.json

ol.gcp.projectId=
ol.gcp.location=
ol.gcp.credentialsFile=
